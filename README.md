# vipul21435.github.io

Source for https://vipul21435.github.io, my portfolio.

One static page: HTML, CSS and a small amount of vanilla JavaScript for the typed hero line, the particle backgrounds, the project filters and the mobile menu. No build step and no dependencies beyond Google Fonts. Every animation stops when the visitor's system asks for reduced motion. GitHub Pages serves `index.html` from `main`.
